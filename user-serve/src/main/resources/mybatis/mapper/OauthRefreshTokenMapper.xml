<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.own.userserve.mvc.mapper.OauthRefreshTokenMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.own.userserve.mvc.entity.OauthRefreshToken">
        <result column="token_id" property="tokenId" />
        <result column="token" property="token" />
        <result column="authentication" property="authentication" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        token_id, token, authentication
    </sql>

</mapper>
